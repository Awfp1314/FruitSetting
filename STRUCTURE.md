# 项目结构说明

## 📁 目录结构

```
laowang-toolbox/
├── .vscode/                    # VS Code 配置
│   ├── extensions.json         # 推荐扩展
│   └── settings.json           # 编辑器设置
├── public/                     # 静态资源
│   ├── manifest.json           # PWA 配置
│   └── sw.js                   # Service Worker
├── src/                        # 源代码
│   ├── components/             # 可复用组件
│   │   ├── AutoTextarea.jsx    # 自动高度文本框
│   │   ├── CopyButton.jsx      # 复制按钮
│   │   ├── StatusBar.jsx       # 状态栏
│   │   ├── Header.jsx          # 页面头部
│   │   ├── ConfigTab.jsx       # 配置标签页
│   │   └── PreviewTab.jsx      # 预览标签页
│   ├── pages/                  # 页面组件
│   │   ├── HomePage.jsx        # 主页
│   │   └── FruitPromoPage.jsx  # 水果促销工具页
│   ├── hooks/                  # 自定义 Hooks
│   │   ├── useInstallPrompt.jsx # PWA 安装提示
│   │   └── useFormData.jsx     # 表单数据管理
│   ├── utils/                  # 工具函数
│   │   ├── clipboard.js        # 剪贴板操作
│   │   └── date.js             # 日期格式化
│   ├── constants/              # 常量配置
│   │   └── defaultData.js      # 默认数据
│   ├── styles/                 # 样式文件
│   │   └── index.css           # 全局样式
│   ├── App.jsx                 # 主应用组件（路由）
│   └── main.jsx                # 应用入口
├── .editorconfig               # 编辑器配置
├── .gitattributes              # Git 属性配置
├── .gitignore                  # Git 忽略文件
├── .prettierrc                 # Prettier 配置
├── CHANGELOG.md                # 更新日志
├── index.html                  # HTML 入口
├── LICENSE                     # 许可证
├── package.json                # 项目配置
├── postcss.config.js           # PostCSS 配置
├── README.md                   # 项目说明
├── STRUCTURE.md                # 本文件
├── tailwind.config.js          # Tailwind 配置
├── vercel.json                 # Vercel 部署配置
└── vite.config.js              # Vite 配置
```

## 🎯 核心文件说明

### 页面层 (Pages)

- **HomePage.jsx**: 应用主页，展示所有工具卡片
- **FruitPromoPage.jsx**: 水果促销文案生成工具

### 组件层 (Components)

- **AutoTextarea**: 自动调整高度的文本框组件
- **CopyButton**: 带状态反馈的复制按钮
- **StatusBar**: 显示网络状态和延迟
- **Header**: 页面头部，包含标题、时间和安装按钮
- **ConfigTab**: 配置表单标签页
- **PreviewTab**: 文案预览标签页

### Hooks 层

- **useFormData**: 管理表单数据和本地存储
- **useInstallPrompt**: 处理 PWA 安装逻辑

### 工具层 (Utils)

- **clipboard.js**: 跨浏览器剪贴板复制
- **date.js**: 日期格式化工具

### 常量层 (Constants)

- **defaultData.js**: 默认表单数据

## 🔄 数据流

```
用户操作
    ↓
页面组件 (HomePage/FruitPromoPage)
    ↓
自定义 Hooks (useFormData/useInstallPrompt)
    ↓
工具函数 (clipboard/date)
    ↓
LocalStorage / Clipboard API
```

## 🎨 样式方案

- **Tailwind CSS**: 主要样式框架
- **index.css**: 全局样式和自定义动画
- **内联样式**: 组件特定样式

## 📦 构建流程

1. Vite 读取 `index.html`
2. 加载 `src/main.jsx` 入口
3. 渲染 `App.jsx` 根组件
4. 根据路由状态显示对应页面
5. Tailwind 处理样式
6. 构建输出到 `dist/` 目录

## 🚀 部署流程

1. 推送代码到 Git 仓库
2. Vercel 检测到更新
3. 执行 `npm install`
4. 执行 `npm run build`
5. 部署 `dist/` 目录
6. 自动生成预览链接

## 🔧 开发建议

### 添加新工具

1. 在 `src/pages/` 创建新页面组件
2. 在 `HomePage.jsx` 的 `tools` 数组添加工具卡片
3. 在 `App.jsx` 添加路由逻辑

### 添加新组件

1. 在 `src/components/` 创建组件文件
2. 保持组件单一职责
3. 使用 Props 传递数据

### 添加新工具函数

1. 在 `src/utils/` 创建工具文件
2. 导出纯函数
3. 添加 JSDoc 注释

## 📝 代码规范

- 使用 ES6+ 语法
- 组件使用函数式组件 + Hooks
- 文件名使用 PascalCase (组件) 或 camelCase (工具)
- 使用 Prettier 格式化代码
- 提交前检查 ESLint
